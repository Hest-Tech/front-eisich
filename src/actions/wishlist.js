/*
	1. Add action creators in this file like so:
	
	export const addToWishlist = () => dispatch => {
		dispatch({
			type: 'ADD_TO_WISHLIST',
			payload: items
		})
	}

	2. Handle the action from reducers.
	3. Call this action from the components. (hint: on event call)
*/
